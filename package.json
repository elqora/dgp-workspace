{
  "name": "@elqora/dgp-workspace",
  "version": "1.0.1",
  "description": "Headless editorial sessions and backend orchestration for the Digital Goods Protocol.",
  "license": "GPL-3.0-only",
  "type": "module",
  "types": "./dist/index.d.ts",
  "module": "./dist/index.js",
  "sideEffects": false,
  "packageManager": "npm@11.6.4",
  "repository": { "type": "git", "url": "git+https://github.com/elqora/dgp-workspace.git" },
  "publishConfig": { "access": "public" },
  "engines": { "node": ">=22" },
  "files": ["dist", "README.md", "LICENSE"],
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }, "./package.json": "./package.json" },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check": "npm run lint && npm run typecheck && npm run test && npm run check:boundaries && npm run build",
    "prepack": "npm run check"
  },
  "dependencies": {
    "@elqora/dgp-core": "^1.0.0",
    "@elqora/dgp-spec": "^1.3.0",
    "@elqora/dgp-validation": "^1.1.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1", "@types/node": "^24.10.0", "eslint": "10.0.1",
    "globals": "^16.5.0", "typescript": "5.9.3", "typescript-eslint": "8.65.0", "vitest": "4.1.10"
  }
}
